class SelectLanguageModel{
  late String title;
  late String subTitle;
  late String character;
   bool isSelected;
  SelectLanguageModel({required this.title, required this.subTitle, required this.character,this.isSelected=false});
}