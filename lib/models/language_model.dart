class SelectLanguageModel{
  late String title;
  late String subTitle;
  late String character;
  late String code;
   bool isSelected;
  SelectLanguageModel({required this.title, required this.subTitle, required this.character,required this.code,this.isSelected=false});
}